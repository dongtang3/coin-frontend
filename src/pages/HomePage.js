import React from 'react';
import { Button } from 'antd';

const HomePage = () => (
  <div>
    <h1>Welcome to My Enterprise App</h1>
    <Button type="primary">Click Me</Button>
  </div>
);

export default HomePage;
